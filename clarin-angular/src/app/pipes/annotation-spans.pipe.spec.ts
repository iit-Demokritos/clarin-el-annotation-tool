import { AnnotationSpansPipe } from './annotation-spans.pipe';

describe('AnnotationSpansPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnotationSpansPipe();
    expect(pipe).toBeTruthy();
  });
});
