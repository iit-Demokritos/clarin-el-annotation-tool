import { AutoannTokenClassifierDirective } from './autoann-token-classifier.directive';

describe('AutoannTokenClassifierDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoannTokenClassifierDirective();
    expect(directive).toBeTruthy();
  });
});
